<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:vm="clr-namespace:SimpleCounter"
             x:Class="SimpleCounter.MainPage">

    <ContentPage.BindingContext>
        <vm:SimpleCounterViewModel />
    </ContentPage.BindingContext>

    <ScrollView>
        <VerticalStackLayout
            Padding="30,0"
            Spacing="25">
            <Image
                Source="dotnet_bot.png"
                HeightRequest="185"
                Aspect="AspectFit"
                SemanticProperties.Description="dot net bot in a submarine number ten" />

            <Label
                Text="{Binding Count}"
                FontSize="48"
                HorizontalOptions="Center" />

            <Button
                x:Name="CounterBtn"
                Text="Increment"
                SemanticProperties.Hint="Increments the counter"
                Command="{Binding IncrementCountCommand}"
                HorizontalOptions="Fill" />

            <Button
                Text="Decrement"
                SemanticProperties.Hint="Decrements the counter"
                Command="{Binding DecrementCountCommand}"
                HorizontalOptions="Fill" />
        </VerticalStackLayout>
    </ScrollView>

</ContentPage>
